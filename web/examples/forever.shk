// 止まらない繰り返しを書いても、ブラウザは固まらない。
// 1回の描画で進める命令の数を、外側（ホスト）が区切っているため
// （spec/runtime/embedding.md）。「止める」で終われる。
func main() -> int {
  var n = 0;
  while true {
    n += 1;
    if n % 200000 == 0 { print(f"{n} まで数えました"); }
  }
  return 0;
}
