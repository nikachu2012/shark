// messy.shk — わざと崩して書いたもの。shark fmt で tidy.expected になる
import std.text;
func   main( )->int{
var n=1+2*3;
    var s   =   "さめ";
      var m = {"a":1,"b":2};
  var xs: list<int> = [1,2,3];
if n > 5 && s.len( ) > 0 {
print(f"{n} {s}");   // 行の終わりのコメントは、空きをそのまま残す
}else{
  print(-n);
}
  for var i in range(3){
        print(i);
  }
        var f=func(a:int,b:int)->int{return a*b; };
  print(f(6,7));
  print(xs[0]+m["a"]);
return 0;
}
